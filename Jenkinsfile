pipeline {
    agent any
    options {
        timestamps()
    }
    environment {
    }
    stages {
        stage('Prepare') {
            steps {
                echo 'Preparing'
            }
        }
    }
}
